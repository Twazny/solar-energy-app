import { CardinalDirectionsPipe } from './cardinal-directions.pipe';

describe('CardinalDirectionsPipe', () => {
  it('create an instance', () => {
    const pipe = new CardinalDirectionsPipe();
    expect(pipe).toBeTruthy();
  });
});
